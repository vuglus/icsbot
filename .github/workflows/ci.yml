name: ICSbot CI

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run application import check
        run: |
          python -c "import flask, requests; print('Imports OK')"

      - name: Run tests
        run: |
          pytest -q

      - name: Build Docker image
        run: |
          docker build -t icsbot:ci .

      - name: Check SSH connectivity
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            echo "SSH connection successful"
            uname -a

      - name: Build Docker image
        run: |
          docker build -t icsbot:ci .

      - name: Save Docker image
        run: |
          docker save icsbot:ci | gzip > icsbot.tar.gz

      - name: Copy image to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          source: "icsbot.tar.gz"
          target: "~/icsbot"

      - name: Deploy Docker on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/icsbot
            docker load < icsbot.tar.gz
            docker stop icsbot || true
            docker rm icsbot || true
            docker run -d \
              --name icsbot \
              -p 5800:5800 \
              -v ~/icsbot/config:/config:ro \
              -e CONFIG_PATH=/config/config.yml \
              --restart unless-stopped \
              icsbot:ci
